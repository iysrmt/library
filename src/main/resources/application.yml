spring:
  application:
    name: library
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library
    username: root
    password: root
  threads:
    virtual:
      enabled: true

server:
  port: 8080
  servlet:
    context-path: /
